

## html
html 파일을 전송하는 프로토콜.
클라이언트에서 서버에 요청을 보내면 서버는 클라이언트가 원하는 결과를 응답한다.

### 특성
- 단방향 통신.
- html 파일을 전송할 목적으로 만들어졌으나, 지금은 json, image 파일 등 또한 전송 가능하다.
- 초기에는 서버는 응답 후 연결을 바로 끊어버렸으나 지금은 성능상의 이유로 연결을 유지 할 수 있다. (keep alive)

## 소켓통신
  두 프로그램이 서로 데이터를 주고 받을 수 있도록 양쪽에 생성되는 통신 단자이다.

- 서버와 클라이언트 양방향 연결이 이루어지는 통신.


  
